#include <stdio.h>

int main() {
    int time;
    scanf("%d", &time);
    int hour = time/3600;
    int min = (time-hour*3600)/60;
    int sec = (time-hour*3600)-min*60;
    printf("%d:%d:%d\n", hour, min, sec);
}
