#include <stdio.h>

int main() {
    float x, y;
    scanf("%f %f", &x, &y);
    if (x <= 0 && y <= 0 && x + y >= -2 || x*x + y*y <= 1) {
        printf("YES");
    }
    else {
        printf("NO");
    }
}
