#include <stdio.h>

int main() {
    float x, y;
    scanf("%f", &x);
    if (x >= 2 || x <= -2) {
        y = 0;
    }
    else if (x > 1) {
        y = 2 - x;
    }
    else if (x < -1) {
        y = -2 - x;
    }
    else {
        y = x;
    }
    printf("%f\n", y);
}
