#include <stdio.h>

int main() {
    const int N = 10;
    int a[N];
    int x = 0;
    while (x != N) {
    scanf ("%d", &a[x]);
    x += 1;
    }
    while (x != 0) {
    printf ("%d ", a[x -= 1]);
    }
}
